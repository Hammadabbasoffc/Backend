<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body class="bg-zinc-800 text-white">
    <div class="flex justify-center flex-col items-center">
        <h1 class="text-4xl font-bold text-center text-white ">Task Manager</h1>
        <div class="w-3/4 mt-5 ">

            <form action="/create" method="post" class="background shadow-md rounded px-8 pt-6 pb-8 mb-4">
                <div class="mb-4">
                    <label class="block text-gray-300 text-sm font-bold mb-2" for="task-title">Task Title</label>
                    <input
                        class=" text-white shadow appearance-none border rounded w-full py-2 px-3 text-white leading-tight outline-none border-none focus:shadow-outline input-background"
                        id="task-title" type="text" placeholder="Write your task" name="title">
                </div>
                <div class="mb-6">
                    <label class="block text-gray-300 text-sm font-bold mb-2" for="task-description">Task
                        Description</label>
                    <textarea
                        class=" text-white shadow resize-none appearance-none border rounded w-full py-2 px-3 text-white leading-tight focus:outline-none border-none focus:shadow-outline input-background"
                        id="task-description" placeholder="Write your description" name="details"></textarea>
                </div>
                <div class="flex items-center justify-between">
                    <input
                        class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                        type="submit">
                </div>
            </form>
        </div>


        <div class="mt-4 w-3/4">
            <h1 class="text-3xl w-full text-center font-semibold mb-4">All My Tasks</h1>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <!-- Task Cards -->


                <% if(files.length> 0) { %>
                    <% files.forEach(val=> { %>
                        <div class="bg-gray-800  rounded-lg p-4 text-center">
                            <h2 class="text-white text-xl font-semibold mb-2 max-w-xs truncate">
                                <%= val.replace(/_/g, ' ').slice(0, -4) %>
                            </h2>
                            <a href="/file/<%= val %>" class="text-blue-500 hover:text-blue-700">Read More</a>
                        </div>
                        <% }); %>
                            <% } else { %>
                                <div class="col-span-3 md:col-span-1 text-center">
                                    <h2 class="text-xl text-gray-400">No Task Available</h2>
                                </div>
                                <% } %>

            </div>
        </div>




    </div>
</body>

</html>